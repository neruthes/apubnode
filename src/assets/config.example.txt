#
# ApubNode repo config file
#
# The syntax is like shell script, but expect no shell evaluation.
# Always quote with doublequotemark
#

sitename="Experimental ApubNode Instance"
http_prefix="http://127.0.0.1:35588/activitypub"
fs_prefix="activitypub/"
path_user="u"
